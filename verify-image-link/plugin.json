{
  "name": "verify-image-link",
  "description": "Verify image link consistency — external refs, broken links, case mismatches, orphans, duplicates",
  "version": "1.0.0",
  "entry": "main.py",
  "triggers": [
    {
      "type": "manual",
      "content": {
        "label": "Verify Image Links",
        "input": [
          {
            "name": "verbose",
            "label": "Verbose (show all images & files)",
            "type": "boolean",
            "default": "true"
          }
        ]
      }
    }
  ]
}
